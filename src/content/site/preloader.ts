import type { SitePreloader } from "@/types/site";

import { siteFooter } from "./footer";

export const sitePreloader: SitePreloader = {
  logoSrc: siteFooter.logoSrc,
  logoAlt: siteFooter.logoAlt,
  slogan: "Arquitectura en madera",
};
